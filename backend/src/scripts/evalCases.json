[
  {
    "owner": "dspinellis",
    "repo": "cscout",
    "issueNumber": 35,
    "expectedFiles": [],
    "note": "VAGUE issue, no clear identifiers ('Add GUI query option to output results as plain text'). Open issue / may have no merged PR. FILL expectedFiles by hand with the files a knowledgeable contributor would edit (e.g. the HTML/output rendering files). expectedFiles takes priority over any PR."
  },
  {
    "owner": "dspinellis",
    "repo": "cscout",
    "issueNumber": 5,
    "expectedFiles": [],
    "note": "VERY VAGUE, empty body ('Complete C99 support'). FILL expectedFiles by hand (e.g. the C99 stub/header files touched by PRs #109/#111). Tests whether the vague->Stage-2 path finds the right area without a PR."
  },
  {
    "owner": "EXAMPLE_owner",
    "repo": "EXAMPLE_repo",
    "issueNumber": 0,
    "expectedFiles": [
      "src/path/to/file_the_human_actually_changed.ts"
    ],
    "note": "GROUND TRUTH PRECEDENCE: (1) expectedFiles if you set it (works for ANY issue, even with no PR) -> (2) else the linked MERGED PR's changed files -> (3) else closed-PR changed files. Prefer hand-labeling expectedFiles so the eval never depends on a PR existing. Analyze the repo in CodeMap first so its graph+retrieval index exist. Aim for 15-30 cases across JS/TS, Python, Go, C/C++. Delete this example before running."
  }
]
